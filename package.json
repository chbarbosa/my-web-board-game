{
  "name": "border-game-monorepo",
  "version": "1.0.0",
  "description": "Full-stack border game project.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start:db": "json-server --watch db.json --port 3002",
    "start:api": "node backend/server.js",
    "start:client": "npm start --prefix frontend",
    "dev": "concurrently \"npm run start:db\" \"npm run start:api\" \"npm run start:client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "react",
    "node",
    "express",
    "fullstack"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
